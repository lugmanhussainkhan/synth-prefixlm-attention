[project]
name = "prefix-lm-trainer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=5.0.1",
    "numpy>=2.5.2",
    "tokenizers>=0.23.1",
    "tqdm>=4.70.0",
]
